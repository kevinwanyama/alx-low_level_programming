#include "stdio.h"

/**
 * main - Entry point
 * @argc: the number of arguments
 * @argv: a string containig the arguments
 * Return: 0
 */

int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
