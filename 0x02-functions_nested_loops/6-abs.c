#include "main.h"
/**
 * _abs: function that computes the absolute value of an integer. compute absoulute value of int
 * @n: the int to check
 * Returns:int abs as the output
 * return: int abs as output
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
