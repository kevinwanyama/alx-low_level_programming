#include <stdio.h>

/**
 * main - prints single dgts
 * prints single digits
 * followed by new line
 * Return 0 always success
 * Return: ALways 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
